# Progress Log

## 2026-08-03 - 주간 데이터 자동 갱신 복구

- 요청: 1235회 추첨 이후에도 앱이 1233회 당첨 데이터와 1234회 추천에 머무는 문제를 수정한다.
- 원인: 생성 데이터 테스트가 현재 pending 레코드를 1234회 legacy v1과 특정 번호로 고정해, 첫 정상 회차 전환에서 `npm run check`가 실패하고 커밋·배포가 건너뛰어졌다.
- 구현:
  - 생성 산출물 테스트를 최신 당첨 회차, 단일 pending 레코드, 유효한 1세트, JS/JSON 일치 등 동적 불변조건 중심으로 변경했다.
  - 체크인된 현재 레코드가 어떤 회차나 지원 엔진 상태에서도 다음 회차로 평가·전환되는 회귀 테스트를 추가했다.
  - 재평가 결과와 저장된 lifecycle/result를 정규화해 비교하여 stale 평가 결과도 검출하도록 했다.
- 검증:
  - `node --test test/generated-weekly-data.test.mjs`: 2개 통과
  - `npm run check`: 전체 16개 테스트 및 주간 사이클 smoke test 통과
  - `git diff --check`: 통과
- 알려진 제한: 추첨 전에 고정·배포되지 못한 1235회 추천은 사후 생성하지 않는다. 복구 실행은 기존 1234회 추천을 평가한 뒤 최신 1235회를 기준으로 1236회 추천을 생성한다.
- 후속 작업: 다음 예약 실행에서도 데이터 생성, 검증, 커밋, 배포가 연속으로 완료되는지 모니터링한다. 향후 엔진 v3 도입 시에는 과거 v2 기록 검증기를 버전별로 보존해야 한다.
