# Progress Log

## 2026-09-20 - 적중 성능 분석과 검증 체계 추가

- 요청: 잦은 0개 적중을 면밀히 분석하고 추천 적중률을 개선한다.
- 분석: 원격의 최신 당첨 1242회와 실제 사전 추천 8회를 가져왔다. 실제 평균은 0.625개, 0개 적중은 37.5%로, 이론 평균 0.8개와 0개 적중 40.06%에 비추어 비정상적 실패라고 판단할 근거가 부족했다.
- 구현: 현행 엔진과 대안 8개를 982회 순차 검증하는 독립 도구, 정확한 무작위 기준과 예상 범위, 앱의 실제 성적/과거 비교 표, 주간 자동 보고서 갱신과 산출물 재현성 검사를 추가했다. 미래 데이터 유입, 표본 제외, 다중 후보 선택의 과적합을 통제하는 검토 기준을 문서화했다.
- 결정: 대폭 적중률 향상은 검증하지 못했다. 어떤 후보도 두 구간의 검토 기준을 통과하지 못해 추천 엔진과 1243회 확정 번호 및 모든 실제 기록을 보존했다. 알고리즘을 개선했다고 주장하지 않는다.
- 업데이트 안정성: 캐시 전환 중 구형 HTML에서 새 성적 영역이 누락되는 오류를 방어하고 제어권 전환 시 새로고침한다. 오프라인에서는 구버전 진입 URL도 캐시된 index로 복구한다. 중간 화면 너비에서 주간 카드가 넘치던 배치도 수정했다.
- 배포: 두 워크플로가 같은 대기열과 검증 절차를 사용하고 배포 이력을 일반 커밋으로 추가하도록 force-push를 제거했다.
- 문서: README, 핵심 로직, 구조 문서와 `docs/recommendation-analysis.md`에 방법·수치·판단·한계를 기록했다.
- 검증: `npm run check` 전체 26개 테스트와 주간 사이클 smoke test 통과. `git diff --check`, 배포 스크립트 `bash -n` 통과. 보고서 재현, 미래 데이터 차단, 구형 앱 셸, 오프라인 진입을 검증했다. 360px 모바일, 1280px 데스크톱에서 실제 성적 표와 접이식 비교를 확인했고, 1024px 화면의 주간 카드 가로 넘침도 수정 후 해소됨을 확인했다. 기존 당첨/추천 데이터 파일에는 diff가 없다.
- 알려진 제한: 과거 검증은 전향적 성능 증명이 아니며 데이터가 늘어나면 구간과 수치가 바뀐다. Galaxy S25가 ADB에서 감지되지 않아 기기 재설치는 연결 확인이 필요하다. 1235회의 누락 추천은 사후 생성하지 않는다.
- 후속 작업: 동일 기준으로 주간 분석을 계속 누적한다. 기준을 통과하는 후보가 생겨도 실제 사용 전에 추첨 전 기록한 추가 검증을 진행한다.

## 2026-08-03 - 주간 데이터 자동 갱신 복구

- 요청: 1235회 추첨 이후에도 앱이 1233회 당첨 데이터와 1234회 추천에 머무는 문제를 수정한다.
- 원인: 생성 데이터 테스트가 현재 pending 레코드를 1234회 legacy v1과 특정 번호로 고정해, 첫 정상 회차 전환에서 `npm run check`가 실패하고 커밋·배포가 건너뛰어졌다.
- 구현:
  - 생성 산출물 테스트를 최신 당첨 회차, 단일 pending 레코드, 유효한 1세트, JS/JSON 일치 등 동적 불변조건 중심으로 변경했다.
  - 체크인된 현재 레코드가 어떤 회차나 지원 엔진 상태에서도 다음 회차로 평가·전환되는 회귀 테스트를 추가했다.
  - 재평가 결과와 저장된 lifecycle/result를 정규화해 비교하여 stale 평가 결과도 검출하도록 했다.
  - 데이터 갱신 관련 코드·테스트·워크플로 변경을 `main`에 push하면 갱신 작업을 즉시 실행하도록 경로 제한 push 트리거를 추가했다. 데이터 파일만 변경하는 자동 커밋은 다시 트리거하지 않는다.
- 검증:
  - `node --test test/generated-weekly-data.test.mjs`: 2개 통과
  - `npm run check`: 전체 16개 테스트 및 주간 사이클 smoke test 통과
  - `git diff --check`: 통과
  - GitHub Actions 복구 실행 `30822301762`: 데이터 생성, 전체 검증, 데이터 커밋, 정적 배포 단계 모두 통과
  - 자동 데이터 커밋 `e403dd7`: 최신 당첨 1235회, 기존 1234회 추천 평가, 현재 추천 대상 1236회 반영
  - 갱신 산출물을 fast-forward한 뒤 `npm run check` 재실행: 전체 16개 테스트 및 smoke test 통과
  - 공개 앱 확인: 1235회 당첨 데이터와 1236회 단일 추천 `3, 4, 5, 18, 28, 40` 표시
- 알려진 제한: 추첨 전에 고정·배포되지 못한 1235회 추천은 사후 생성하지 않는다. 복구 실행은 기존 1234회 추천을 평가한 뒤 최신 1235회를 기준으로 1236회 추천을 생성한다.
- 후속 작업: 다음 예약 실행에서도 데이터 생성, 검증, 커밋, 배포가 연속으로 완료되는지 모니터링한다. 향후 엔진 v3 도입 시에는 과거 v2 기록 검증기를 버전별로 보존해야 한다.
